---
created: 2026-01-08
tags: [type/api-reference, api/user]
api_name: Vercel API
category: user
endpoint_count: 3
---

# Vercel API - User

[[README|Back to Overview]]

---

## GET /v3/events

**List User Events**

Retrieves a list of "events" generated by the User on Vercel. Events are generated when the User performs a particular action, such as logging in, creating a deployment, and joining a Team (just to name a few). When the `teamId` parameter is supplied, then the events that are returned will be in relation to the Team that was specified.

### Parameters

| Name | In | Type | Required | Description |
|------|-----|------|----------|-------------|
| `limit` | query | number | No | Maximum number of items which may be returned. |
| `since` | query | string | No | Timestamp to only include items created since then |
| `until` | query | string | No | Timestamp to only include items created until then |
| `types` | query | string | No | Comma-delimited list of event \"types\" to filter  |
| `userId` | query | string | No | Deprecated. Use `principalId` instead. If `princip |
| `principalId` | query | string | No | When retrieving events for a Team, the `principalI |
| `projectIds` | query | string | No | Comma-delimited list of project IDs to filter the  |
| `withPayload` | query | string | No | When set to `true`, the response will include the  |
| `teamId` | query | string | No | The Team identifier to perform the request on beha |
| `slug` | query | string | No | The Team slug to perform the request on behalf of. |

### Example

```bash
curl -X GET \
  -H "Authorization: Bearer $TOKEN" \
  "https://api.vercel.com/v3/events"
```

---

## GET /v2/user

**Get the User**

Retrieves information related to the currently authenticated User.

### Example

```bash
curl -X GET \
  -H "Authorization: Bearer $TOKEN" \
  "https://api.vercel.com/v2/user"
```

---

## DELETE /v1/user

**Delete User Account**

Initiates the deletion process for the currently authenticated User, by sending a deletion confirmation email. The email contains a link that the user needs to visit in order to proceed with the deletion process.

### Example

```bash
curl -X DELETE \
  -H "Authorization: Bearer $TOKEN" \
  "https://api.vercel.com/v1/user"
```

---

---
created: 2026-01-09
tags: [type/api-reference, api/team]
api_name: Slack Web API
category: team
endpoint_count: 4
---

# Slack Web API - Team

[[README|Back to Overview]]

---

## GET /team.accessLogs

Gets the access logs for the current team.

### Parameters

| Name | In | Type | Required | Description |
|------|-----|------|----------|-------------|
| `token` | query | string | Yes | Authentication token. Requires scope: `admin` |
| `before` | query | string | No | End of time range of logs to include in results (i |
| `count` | query | string | No |  |
| `page` | query | string | No |  |

### Example

```bash
curl -X GET \
  -H "Authorization: Bearer $TOKEN" \
  "/team.accessLogs"
```

---

## GET /team.billableInfo

Gets billable users information for the current team.

### Parameters

| Name | In | Type | Required | Description |
|------|-----|------|----------|-------------|
| `token` | query | string | Yes | Authentication token. Requires scope: `admin` |
| `user` | query | string | No | A user to retrieve the billable information for. D |

### Example

```bash
curl -X GET \
  -H "Authorization: Bearer $TOKEN" \
  "/team.billableInfo"
```

---

## GET /team.info

Gets information about the current team.

### Parameters

| Name | In | Type | Required | Description |
|------|-----|------|----------|-------------|
| `token` | query | string | Yes | Authentication token. Requires scope: `team:read` |
| `team` | query | string | No | Team to get info on, if omitted, will return infor |

### Example

```bash
curl -X GET \
  -H "Authorization: Bearer $TOKEN" \
  "/team.info"
```

---

## GET /team.integrationLogs

Gets the integration logs for the current team.

### Parameters

| Name | In | Type | Required | Description |
|------|-----|------|----------|-------------|
| `token` | query | string | Yes | Authentication token. Requires scope: `admin` |
| `app_id` | query | string | No | Filter logs to this Slack app. Defaults to all log |
| `change_type` | query | string | No | Filter logs with this change type. Defaults to all |
| `count` | query | string | No |  |
| `page` | query | string | No |  |
| `service_id` | query | string | No | Filter logs to this service. Defaults to all logs. |
| `user` | query | string | No | Filter logs generated by this user’s actions. Defa |

### Example

```bash
curl -X GET \
  -H "Authorization: Bearer $TOKEN" \
  "/team.integrationLogs"
```

---
